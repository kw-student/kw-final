services:
  backend:
    build: ./back
    ports:
      - "8000:8000"

  frontend:
    build: ./front
    ports:
      - "8501:8501"
    environment:
      - BACKEND_URL=http://backend:8000
    depends_on:
      - backend
